import { type } from "os";
import { types } from "util";

export type homeState = {
  showAdddtional: boolean;
  loading: boolean;
  downloadFormat: string;
  url: string;
  imageUrl: string;
  width: string;
  height: string;
};
